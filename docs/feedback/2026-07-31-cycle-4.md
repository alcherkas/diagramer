# Feedback — Cycle 4 (2026-07-31)

Product-loop pass over https://alcherkas.github.io/diagramer/ after
REQ-003 + REQ-012 shipped (Deploy green).

## What was tested

- Full REQ-003 acceptance-criteria walk and REQ-012 occlusion scenario on the
  live app.

## Verification results

- **REQ-003 → verified.** Empty state renders; label/description edits update
  the canvas live; Backspace while typing does not delete the node; diagram
  name edits persist across reload; error toast auto-dismisses in ~4s.
- **REQ-012 → verified.** Reproduced the cycle-3 occlusion layout (target
  down-right of source, label midpoint under the node) — the label is now
  clickable and editable in place.

## Bugs

None found.

## UX issues

- None new. The only notable gap remains reachability of container/component
  diagrams (REQ-011, already `ready`).

## Opportunities

- Boundary nesting (REQ-004) is the natural next feature: boundaries render
  since REQ-001 but do nothing yet, and they only become reachable in the UI
  together with REQ-011 — implementing REQ-004 then REQ-011 makes container
  diagrams fully usable.

## Backlog changes

- REQ-003: done → **verified**; REQ-012: done → **verified**.
- No new requirements this cycle. Next up for engineering: REQ-004.
